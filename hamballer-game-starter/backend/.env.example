# HamBaller.xyz Backend Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (Supabase)
SUPABASE_URL=https://your_supabase_project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key

# Blockchain Configuration
ABSTRACT_RPC_URL=https://api.testnet.abs.xyz
HODL_MANAGER_ADDRESS=0x... # HODLManager contract address
DBP_TOKEN_ADDRESS=0x... # DBP Token contract address
BOOST_NFT_ADDRESS=0x... # BoostNFT contract address

# XPBadge NFT Configuration

# XPVerifier ZK-Proof Configuration
XPVERIFIER_ADDRESS=0x... # XPVerifier contract address for ZK-SNARK proof verification
XPVERIFIER_PRIVATE_KEY=0x... # Private key for XPVerifier interactions (optional, can use XPBADGE_MINTER_PRIVATE_KEY)
ZK_MASTER_SECRET=your_master_secret_for_nullifier_generation # Master secret for deterministic nullifier generation (keep secure!)
XPBADGE_ADDRESS=0x... # XPBadge contract address
XPBADGE_MINTER_PRIVATE_KEY=0x... # Private key for XPBadge minting (should have MINTER_ROLE)

# Optional: API Keys for external services
ETHERSCAN_API_KEY=your_etherscan_api_key

# Security (optional)
CORS_ORIGIN=http://localhost:3000
API_RATE_LIMIT=100

# Logging
LOG_LEVEL=info
